* better diagram of simple state flow
* tell students that you can only return one DOM element in React unless you're using v 16
* controlled v uncontrolled components
* synthetic events
* display (dumb/stateless) components v reg components
* functional syntax for dumb components 
* pure components
* deconstructing vars (props, state)
* conditional displaying of component {condition && <Component />}
* prop types
